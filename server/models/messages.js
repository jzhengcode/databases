var db = require('../db');

module.exports = {
  getAll: function () {
    // create query string
    // select messages.text, messages.room_id, rooms.name, users.user_name
  }, // a function which produces all the messages
  create: function () {} // a function which can be used to insert a message into the database
};
